<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CustomEditText" parent="Theme.AppCompat.Light">
        <item name="android:padding">@dimen/dp_sixteen</item>
        <item name="android:textSize">@dimen/sp_fourteen</item>
        <item name="android:layout_margin">@dimen/dp_eight</item>
        <item name="android:ems">10</item>
    </style>

    <style name="CustomButton" parent="Theme.AppCompat.Light">
        <item name="android:textSize">@dimen/sp_fourteen</item>
        <item name="android:layout_margin">@dimen/dp_eight</item>
        <item name="colorControlHighlight">@color/colorPrimary</item>
        <item name="colorButtonNormal">@color/colorAccent</item>
    </style>

</resources>
